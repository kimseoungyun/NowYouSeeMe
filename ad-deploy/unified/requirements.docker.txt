pydub
gTTS
ffmpeg-python
pandas
google-genai
google-generativeai
openai
python-dotenv  # .env 파일 로드
openai-whisper  # Whisper STT for extract_for_gpt.py
yt-dlp
moviepy

# === TTS with Coqui XTTS ===
coqui-tts
numpy>=1.22.0
hangul_romanize>=0.1.0  # Required for Korean TTS

# === Audio Processing for Ducking ===
pyloudnorm>=0.1.0
scipy>=1.9.0
torchaudio>=2.0.0
torch>=2.0.0
librosa>=0.10.0  # Audio resampling for VAD
soundfile>=0.12.0  # Audio file I/O (used by librosa and pydub)

# === Silero VAD (loaded via torch.hub, but torch is required) ===
# Note: Silero VAD model is automatically downloaded when first used
